[workspace]

[package]
name = "jev-contract"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"
description = "Typed, transport-independent contract for the JEV evaluation worker"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "0.8"
