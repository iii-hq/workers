iii: v1
name: jev
language: rust
deploy: binary
manifest: Cargo.toml
license: Apache-2.0
bin: jev
tags: [evaluation, noul, choice, score, typesafe, relevance]
description: TypeSafe evaluation (jev::evaluate), model listing (jev::models::list) and caller-scoped cancellation (jev::cancel), with typed usage, provider errors, per-call retries and worker-owned credentials.
config:
  api_key: null
  model: jev-1.13.0
  max_request_bytes: 8388608
  max_response_bytes: 8388608
  max_timeout_ms: 300000
dependencies:
  configuration: "latest"
