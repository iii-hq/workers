iii: v1
name: session
language: rust
deploy: binary
manifest: Cargo.toml
bin: iii-session
description: Session storage (parent-id tree under session-tree::*) and per-session inbox (session-inbox::*) backed by iii state.

runtime:
  kind: rust

scripts:
  install: cargo build
  start: cargo run
