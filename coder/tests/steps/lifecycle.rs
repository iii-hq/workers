//! Placeholder for the end-to-end lifecycle scenario. The scenario in
//! [`tests/features/lifecycle.feature`](../../features/lifecycle.feature)
//! reuses the generic dispatcher and assertions from
//! [`common`](super::common), [`read`](super::read),
//! [`list`](super::list), [`tree`](super::tree), and
//! [`search`](super::search); no lifecycle-only steps are needed.
