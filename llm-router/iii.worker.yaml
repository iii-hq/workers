iii: v1
name: llm-router
language: rust
deploy: binary
manifest: Cargo.toml
bin: llm-router
description: One front door + provider protocol in front of every LLM provider.

dependencies:
  iii-state: "^0.21.3"
  configuration: "^0.21.3"
