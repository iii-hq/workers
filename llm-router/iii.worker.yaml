iii: v1
name: llm-router
language: rust
deploy: binary
manifest: Cargo.toml
bin: llm-router
description: One front door + provider protocol in front of every LLM provider.

dependencies:
  state: "^0.21.0"
  configuration: "^0.21.6"
