iii: v1
name: models-catalog
language: rust
deploy: binary
manifest: Cargo.toml
bin: models-catalog
description: Model capabilities knowledge base under models::* (list/get/supports/register).

runtime:
  kind: rust

scripts:
  install: cargo build
  start: cargo run
