iii: v1
name: compose-ui
language: javascript
deploy: bundle
manifest: package.json
license: Apache-2.0
tags: [compose, console, ui, operations, supervisor, logs]
description: Compose project supervision in the Console — a page over compose::* with live container state, lifecycle actions, worker packages, and per-container log tails.

runtime:
  kind: javascript

scripts:
  start: node ./index.mjs

dependencies:
  console: "^1.9.14"
