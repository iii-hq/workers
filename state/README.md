# state

Distributed key-value state management with reactive change triggers for iii — registers the `state` trigger type and the `state::*` functions, migrating the engine's built-in `iii-state` into a standalone worker.
