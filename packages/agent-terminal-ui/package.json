{
  "name": "@iii-workers/agent-terminal-ui",
  "version": "0.1.0",
  "private": true,
  "description": "The console terminal page every agent-CLI worker injects: one full-pane xterm bound to a shell::pty session that runs that agent's program. Source-only — each worker bundles it into its own page asset with its own worker name and labels.",
  "type": "module",
  "main": "./src/index.tsx",
  "exports": {
    ".": "./src/index.tsx",
    "./styles.css": "./src/styles.css"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@iii-dev/console-ui": "workspace:*",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "react": "^19.2.8",
    "typescript": "^5.9.2"
  }
}
