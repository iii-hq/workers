# pubsub worker

Topic-based publish/subscribe messaging for iii. Registers the `subscribe` trigger type and the `publish` service function, replacing the engine builtin `iii-pubsub`. Full docs land in Task 10.
