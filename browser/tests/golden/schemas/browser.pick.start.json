{
  "description": "Internal: enter DevTools inspect mode so the human can pick an element in the console UI. The pick arrives as a browser::picked trigger event. Not an agent function.",
  "function_id": "browser::pick::start",
  "request_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
      "session_id": {
        "type": "string"
      }
    },
    "required": [
      "session_id"
    ],
    "title": "PickStartInput",
    "type": "object"
  },
  "response_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
      "ok": {
        "type": "boolean"
      }
    },
    "required": [
      "ok"
    ],
    "title": "PickOutput",
    "type": "object"
  }
}
