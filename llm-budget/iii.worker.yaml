iii: v1
name: llm-budget
language: rust
deploy: binary
manifest: Cargo.toml
bin: llm-budget
description: Workspace + agent LLM spend caps with alerts, forecast, and period rollover under budget::*.

runtime:
  kind: rust

scripts:
  install: cargo build
  start: cargo run
