<!-- generated by iii-skill-render — DO NOT EDIT (changes here are overwritten on the next render). Edit docs/leaves/list.md. -->

# Discovering budgets in a workspace

## When to use

- Enumerating budgets before deciding which one to attach to a new agent or workflow.
- Auditing all budgets in a workspace to track spend across cost centres.
- Building a dashboard that needs to enumerate active and paused budgets together.

## Notes

- Results are sorted by `created_at` descending, so newly created budgets appear first.
- Each entry includes `spent_usd`, `ceiling_usd`, `enforced`, `paused`, and period boundary timestamps, which is enough to assess status without a follow-up `budget::get`.
- There is no pagination; for large deployments, filter by `workspace_id` to bound the response size.
