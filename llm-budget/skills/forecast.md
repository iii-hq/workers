<!-- generated by iii-skill-render — DO NOT EDIT (changes here are overwritten on the next render). Edit docs/leaves/forecast.md. -->

# Projecting end-of-period spend

## When to use

- Determining whether a budget is on track to stay under the ceiling before the period resets.
- Alerting an operator early — say, at 50% elapsed time — that the projected total will overshoot.
- Driving a spend forecast widget that surfaces estimated end-of-period cost.

## Notes

- The daily rate comes from the current period only (`spent_usd / days_elapsed`). Archived periods are not included, so a freshly created or just-reset budget can produce a noisy projection from a small sample.
- `projected_month_usd` is always a 30-day projection regardless of the budget's `period` setting.
- `days_until_breach` is absent when the rate is zero (no spend yet) or when there is no remaining headroom (already over the ceiling).
- For actual historical spend rather than a projection, use `budget::usage`.
