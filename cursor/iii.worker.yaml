iii: v1
name: cursor
language: javascript
deploy: bundle
manifest: package.json
license: Apache-2.0
tags: [llm, provider, coding-agent, cursor, acp, bridge, local-agent, cloud-agent]
description: Cursor as a login-backed, text-only LLM Router provider and coding-agent worker through official CLI ACP, with optional sdk.v1 Bridge sessions.

runtime:
  kind: javascript

scripts:
  start: node ./index.mjs

dependencies:
  state: "0.x"
  iii-stream: "0.x"
  configuration: "0.x"
  llm-router: "^1.4.12"
