iii: v1
name: document
language: rust
deploy: binary
manifest: Cargo.toml
license: Apache-2.0
bin: document
tags: [document, markdown, docx, pptx, xlsx, epub, csv, attachments, text-extraction]
description: Convert Word, PowerPoint, Excel, OpenDocument, RTF, EPUB, CSV and PDF documents to markdown on this machine, detect the format from the bytes, and pull out the images embedded in them.
dependencies:
  configuration: "^0.21.6"
