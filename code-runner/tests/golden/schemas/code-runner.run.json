{
  "function_id": "code-runner::run",
  "description": "Run code in a sandbox (no network, host filesystem or packages). One-shot by default: nothing persists and no runtime_id is returned. keep: true keeps the runtime and returns its runtime_id to reuse until code-runner::teardown or the idle TTL reaps it. A failing script is a response, not an error.",
  "request_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RunRequest",
    "type": "object",
    "required": [
      "code"
    ],
    "properties": {
      "code": {
        "description": "Node: an async function body (`return x` sets `result`; `await` works). Python: a module (assign `result = x`). A global `iii` is in scope.",
        "type": "string"
      },
      "keep": {
        "description": "When `runtime_id` is omitted: `true` leaves the runtime running and returns its `runtime_id`; `false` destroys it after the run.",
        "default": false,
        "type": "boolean"
      },
      "lang": {
        "description": "Required when `runtime_id` is omitted; on an existing runtime omit it or pass the runtime's own language.",
        "default": null,
        "anyOf": [
          {
            "$ref": "#/definitions/Lang"
          },
          {
            "type": "null"
          }
        ]
      },
      "runtime_id": {
        "description": "Run in this existing runtime (from a keep: true run), sharing its globals and scratch; it is not destroyed afterwards. Omit to run one-shot.",
        "default": null,
        "type": [
          "string",
          "null"
        ]
      },
      "timeout_ms": {
        "description": "Wall-clock budget in milliseconds, clamped to the configured maximum.",
        "default": null,
        "type": [
          "integer",
          "null"
        ],
        "format": "uint64",
        "minimum": 0.0
      }
    },
    "definitions": {
      "Lang": {
        "description": "Which engine backs a runtime.\n\nOnly `node` and `python` are accepted; anything else fails deserialization before a handler runs, so the schema itself is the validation.",
        "type": "string",
        "enum": [
          "node",
          "python"
        ]
      }
    }
  },
  "response_schema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "RunResponse",
    "type": "object",
    "required": [
      "duration_ms",
      "exit_code",
      "result",
      "stderr",
      "stdout",
      "success"
    ],
    "properties": {
      "duration_ms": {
        "type": "integer",
        "format": "uint64",
        "minimum": 0.0
      },
      "exit_code": {
        "type": "integer",
        "format": "int64"
      },
      "result": {
        "description": "The completion value: what the code returned (node) or assigned to `result` (python). Null when it returned nothing, when it returned something JSON cannot represent, or when the run failed.\n\nAlways present, never skipped — unlike `runtime_id`, a null result is information (\"the code returned nothing\") rather than an absence."
      },
      "runtime_id": {
        "description": "Present when this run addresses a runtime that outlives the call — a `keep: true` run that SUCCEEDED, or a `runtime_id` echoed back. Absent, not null, otherwise: there is nothing to address. Treat a present value as a secret.",
        "type": [
          "string",
          "null"
        ]
      },
      "stderr": {
        "type": "string"
      },
      "stdout": {
        "type": "string"
      },
      "success": {
        "type": "boolean"
      }
    }
  }
}
