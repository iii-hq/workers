iii: v1
name: auth-credentials
language: node
deploy: binary
manifest: package.json
bin: iii-auth-credentials
description: Multi-provider credential store on the iii bus (auth::*).

runtime:
  kind: node

scripts:
  install: pnpm install
  start: node ./dist/auth-credentials/main.js --config ./config.yaml
