iii: v1
name: llm-budget
language: node
deploy: binary
manifest: package.json
bin: iii-llm-budget
description: Workspace + agent LLM spend caps with alerts, forecast, and period rollover under budget::*.

runtime:
  kind: node

scripts:
  install: pnpm install
  start: node ./dist/llm-budget/main.js

dependencies:
  iii-state: "^0.11.0"
