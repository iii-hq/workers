iii: v1
name: approval-gate
language: node
deploy: binary
manifest: package.json
bin: iii-approval-gate
description: Owns approval state. Registers approval::resolve and a state-trigger adapter on the approvals scope that wakes turn::step on every decision write.

runtime:
  kind: node

scripts:
  install: pnpm install
  start: node ./dist/approval-gate/main.js --config ./config.yaml
