iii: v1
name: tailscale
language: rust
deploy: binary
manifest: Cargo.toml
license: Apache-2.0
bin: tailscale
tags: [tailscale, tailnet, vpn, remote-access, serve, funnel, taildrop, exit-node, dns, console]
description: Tailscale as an iii worker — tailscale::* functions for connectivity and peers, exit nodes and preferences, Serve and Funnel publishing, Taildrop, certificates, Taildrive, accounts, tailnet lock, and updates, plus a Console page with QR links.

dependencies:
  configuration: "0.x"
  console: "^1.9.14"
