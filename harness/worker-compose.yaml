namespace: my-project #comment if you want to be default
startup_timeout: 15m
stop_timeout: 10s

engine:
  url: ws://127.0.0.1:49134

containers:
  queue:
    worker: path://../queue
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin queue

  state:
    worker: path://../state
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin state

  session-manager:
    worker: path://../session-manager
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin session-manager

  llm-router:
    worker: path://../llm-router
    start_after:
      - state
    environment:
      RUST_LOG: info
      ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY:-}"
      OPENAI_API_KEY: "${OPENAI_API_KEY:-}"
    scripts:
      run: cargo run --locked --bin llm-router

  provider-openai-codex:
    worker: path://../provider-openai-codex
    start_after:
      - llm-router
      - state
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin provider-openai-codex

  provider-openai:
    worker: path://../provider-openai
    start_after:
      - llm-router
      - state
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin provider-openai

  provider-anthropic:
    worker: path://../provider-anthropic
    start_after:
      - llm-router
      - state
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin provider-anthropic

  context-manager:
    worker: path://../context-manager
    start_after:
      - llm-router
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin context-manager

  iii-directory:
    worker: path://../iii-directory
    environment:
      RUST_LOG: info
      ORT_PREFER_DYNAMIC_LINK: "0"
      ORT_CXX_STDLIB: stdc++
    scripts:
      # iii-directory links the pinned static ONNX Runtime; provision once with
      # ../iii-directory/scripts/provision-onnxruntime.sh (or point ORT_LIB_PATH).
      run: >-
        ORT_LIB_PATH="${ORT_LIB_PATH:-$HOME/.cache/iii/onnxruntime-static-1.28.0-x86_64-unknown-linux-gnu}"
        cargo run --locked --bin iii-directory

  cron:
    worker: path://../cron
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin cron

  console:
    worker: path://../console
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin console

  shell:
    worker: path://../shell
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin shell

  harness:
    worker: path://.
    start_after:
      - state
      - queue
      - session-manager
      - llm-router
      - provider-openai-codex
      - provider-openai
      - provider-anthropic
      - context-manager
      - iii-directory
      - cron
      - console
      - shell
    environment:
      RUST_LOG: info
    scripts:
      run: cargo run --locked --bin harness
  # added by compose::add
