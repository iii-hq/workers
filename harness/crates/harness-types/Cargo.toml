[package]
name = "harness-types"
description = "Shared data types for harness: AgentMessage, AgentEvent, AgentTool, AgentContext, stream events"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/iii-hq/workers"
authors = ["iii contributors"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
