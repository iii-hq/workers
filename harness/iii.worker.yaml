iii: v1
name: harness
language: javascript
deploy: bundle
manifest: package.json
description: Meta-worker that composes the modular Node workers backing the iii chat surface.

runtime:
  kind: javascript

scripts:
  start: node ./index.mjs

dependencies:
  iii-state: "^0.11.0"
  iii-stream: "^0.11.0"
  iii-directory: "^0.5.1"
  configuration: "^0.11.0"
