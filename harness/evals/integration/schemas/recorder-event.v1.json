{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "RecorderEventKind": {
      "enum": [
        "target_call",
        "lifecycle"
      ],
      "type": "string"
    },
    "SchemaVersion1": {
      "description": "The literal string `\"1\"`; any other value is a schema error.",
      "enum": [
        "1"
      ],
      "type": "string"
    }
  },
  "properties": {
    "function_id": {
      "type": "string"
    },
    "kind": {
      "$ref": "#/definitions/RecorderEventKind"
    },
    "payload": true,
    "received_at": {
      "description": "RFC 3339 receipt time — diagnostic, never an oracle input.",
      "type": "string"
    },
    "run_id": {
      "type": "string"
    },
    "schema_version": {
      "$ref": "#/definitions/SchemaVersion1"
    },
    "sequence": {
      "description": "Strictly increasing; assigned before the handler responds.",
      "format": "uint64",
      "minimum": 0.0,
      "type": "integer"
    }
  },
  "required": [
    "function_id",
    "kind",
    "payload",
    "received_at",
    "run_id",
    "schema_version",
    "sequence"
  ],
  "title": "RecorderEventV1",
  "type": "object"
}
