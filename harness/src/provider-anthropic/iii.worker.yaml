iii: v1
name: provider-anthropic
language: node
deploy: binary
manifest: package.json
bin: iii-provider-anthropic
description: Anthropic Messages API streaming provider; exposes provider::anthropic::stream on the iii bus.

runtime:
  kind: node

scripts:
  install: pnpm install
  start: node ./dist/provider-anthropic/main.js --config ./config.yaml

dependencies:
  configuration: "^0.11.0"
