iii: v1
name: approval-gate
language: node
deploy: binary
manifest: package.json
bin: iii-approval-gate
description: Registers approval::resolve; routes decisions to per-call turn::approval_resume functions owned by the turn-orchestrator.

runtime:
  kind: node

scripts:
  install: pnpm install
  start: node ./dist/approval-gate/main.js --config ./config.yaml
