iii: v1
name: approval-gate
language: node
deploy: binary
manifest: package.json
bin: iii-approval-gate
description: Registers approval::resolve; persists human decisions to the approvals scope (turn-orchestrator reacts via turn::on_approval).

runtime:
  kind: node

scripts:
  install: pnpm install
  start: node ./dist/approval-gate/main.js --config ./config.yaml
