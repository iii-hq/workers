# The jailed lane only overrides the engine listener for test isolation. Its
# shell settings live in shell-jailed.yaml and go directly to the host worker.
workers:
  - name: iii-worker-manager
    config:
      host: 127.0.0.1
      port: ${E2E_PORT:49134}
